<% content_for :title, "Editing product" %>

<h1>Editing product</h1>

<%= render "form", product: @product %>

<br>

<div class="columns mt-4 flex items-center justify-between">
  <div class="flex gap-2">
    <%= link_to "Show this product", @product, class: "button is-small is-info" %>
  </div>
  <div class="flex gap-2">
    <%= link_to "Back to products", products_path, class: "button is-small is-outlined" %>
  </div>
</div>
